fix(demo): book pipeline + Station7 work on plain HTTP (sha256 + runes reactivity)

Two create-flow blockers found by Playwright-walking all 7 stations:

1. Station 6 Seal crashed ("n is not a function"): BookAssembler.sha256Hex +
   WorkshopBookPipeline.blobHash hashed via crypto.subtle (undefined in a
   non-secure HTTP context) then node:crypto (absent in browser). New pure-JS
   $lib/util/sha256.ts (subtle when secure, FIPS-180-4 JS otherwise -- identical
   digest). BROWSER-VERIFIED: Seal now generates the book end-to-end
   (title "Ada and the Runaway Sock", 17pp, PDF 157.9KB, hash 685bf956...).

2. Station 7 print CTA was dead: phase + errorMsg were plain 'let' in a Svelte-5
   runes component ($props/$state), so reassignment never re-rendered. Made both
   $state -- unblocks address->quote->pay(test-mode 4242)->success. Empty Stripe
   key already degrades to test-mode card (decideStripePath).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
