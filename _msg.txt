fix(demo): block in-browser Ollama hop in $lib/llr (no-Ollama-prod mandate)

Station 6 Seal logged 3 console 404s per generation: the browser's
llm.chat -> resolveStoryLlmProvider() -> OllamaProvider POSTed to
http://localhost:11434/api/chat. In a remote demo localhost:11434 is the
USER's machine (never our server), and the no-Ollama-prod rule forbids the
hop in shipped builds. This repo wires no in-browser/WebGPU StoryLLM
provider, so llm.chat now throws SYNCHRONOUSLY in the browser when the
resolved provider is Ollama (no network, no 404s) and StoryAuthorService
falls straight through to its deterministic template author. Ollama stays a
Node dev/CI failsafe (window === undefined in SSR/test/CLI).

BROWSER-VERIFIED: Seal console went 4 errors -> 1 (only the benign
pillar-library 404, handled by the v2 fallback); book still generates
("Ada and the Runaway Sock", 17pp).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
