polish(demo): per-route titles + meta/OG, fix double-title, mobile pill clearance

D1 titles/meta:
- /examples and /examples/[id] had NO <svelte:head> — added titles (the reader
  uses the book title) + meta descriptions.
- Landing (/) gained a descriptive title + meta description + OpenGraph tags.
- /library title got the brand suffix.
- ROOT FIX: src/app.html hardcoded <title>Storybook Workshop</title> BEFORE
  %sveltekit.head%, so every page rendered two <title> tags and browsers used
  the first — the tab title was stuck on "Storybook Workshop" everywhere.
  Removed the static title; svelte:head titles are now authoritative. Verified
  each route shows exactly its own title (curl SSR + browser document.title for
  the client-rendered landing).

D3 mobile: fixed nav pills (position:fixed top-right) occluded page <h1>s on
narrow screens. Wrapped layout children and added a mobile-only (<=600px) top
padding so headings clear the pills. Verified landing + /styles at 375px.

D2 a11y: confirmed already correct — the 150 Station-2 archetype tiles are
<button aria-label={pillarLabel}> with a decorative <img alt="">, which is the
right pattern (no double announce). No change.

Gates 11/11, build green.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
