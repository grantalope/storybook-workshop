# step-1.md\n\nReconcile partial work (AC5). git diff the two modified routes; `git checkout --` the shipping hunk (REJECTED per A-P0-3); review untracked money-integrity.test.ts, keep the FILE but fix per A-P1-2 (server-level match) or stash aside to re-add in step 3; leave tasks/codex-runs/ alone. Commit `chore(bh-a): reconcile prior partial fix (shipping hunk rejected per red-team P0-3)` with per-hunk decisions in body.\nRe-read ../notes-from-the-boss.md first.\n