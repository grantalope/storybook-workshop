# step-2.md\n\nAtomic webhook dedup (AC1, AC2). Schema v2 (processed_webhook_events + refund_ledger tables; migration v1->v2 idempotent + tests). WebhookOrderStore capability + adapters (A-P1-5). applyStripeWebhookEventOnce atomic op per A-P0-2. Route: all 3 event types through it; ack matrix per A-P1-7/P2-1. Tests incl Promise.all concurrency on both stores + ordering. Commit.\nRe-read ../notes-from-the-boss.md first.\n