# step-4.md\n\nRefund execution (AC4). Refund ledger store ops (both stores, in schema v2 from step 2). StripeCheckoutService.refund sends deterministic Idempotency-Key. New POST /api/quality-claim/[id]/decision: OPS_API_TOKEN bearer (401 otherwise; production-config assert). pending-before-call; replay -> alreadyExecuted; failure -> ledger failed + 502. Parent submit untouched. Tests. Commit.\nRe-read ../notes-from-the-boss.md first.\n